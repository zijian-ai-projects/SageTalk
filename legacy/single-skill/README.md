# Legacy Single-Skill Layout

This directory preserves the former root `SKILL.md` content during the migration to repo-level
Codex skills under `.agents/skills/`.

It is not the canonical runtime path. New work should target `.agents/skills/sage-talk/` and the
independent `.agents/skills/sage-*` directories.
